[workspace]
members = ["interp", "lock-anchors/wasm", "scored26-release", "vouch"]
resolver = "2"

[profile.release]
codegen-units = 1
incremental = false
lto = "fat"
panic = "abort"
strip = "symbols"

[profile.release.package.lispex-wasm]
opt-level = "s"
